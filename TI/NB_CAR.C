char   *fillstr;        /* NB_CAR_PER_LINE + 2;
char   *clippbuf;       /* NB_CAR_PER_LINE * 2;
char   *lineTab;        /* NB_CAR_PER_LINE * 2;
char   *linebuf;        /* NB_CAR_PER_LINE * 2; /* tampon ligne             */
char   *bufline;        /* NB_CAR_PER_LINE;     /* buffer entree */
char   *buftoken;       /* NB_CAR_PER_LINE;     /* buffer ou sont stockes les tokens */
char   *shbuf;          /* NB_CAR_PER_LINE;        /* buffer du shell                   */