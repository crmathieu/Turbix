/* libc.c */

/*---------------------------------------------------------------------------
 * strcmp - compares 2 strings. return 0 when equal
 *---------------------------------------------------------------------------
 */
strcmp(str1, str2)
char *str1, *str2;
{
    while(*str1 == *str2++) {
        if (*str1++ == '\0') {
            return(0);
        }
    }
    return(*str1 - *(--str2));
}

/*---------------------------------------------------------------------------
 * strlen - returs a string length
 *---------------------------------------------------------------------------
 */
strlen(string)
char *string;
{
  int i = 0;
  while(*(string++)) { i++; }
  return(i);
}
